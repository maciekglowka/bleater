{% extends "base.jinja" %}
{% block content %}

<h2>User list</h2>

<ul>
  {% for user in users %}
  <li>
    <a href="/user?id={{ user.id }}">{{ user.name }}</a>
  </li>
  {% endfor %}
</ul>

{% endblock %}
